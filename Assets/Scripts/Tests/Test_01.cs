using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.InputSystem;

public class Test_01 : Test
{
    public Vector2 pos;

    //protected override void Test1(InputAction.CallbackContext context)
    //{
    //    PoolManager.Inst.GetObject(Shape.Circle, transform.position);
    //}

    //protected override void Test2(InputAction.CallbackContext context)
    //{
    //    PoolManager.Inst.GetObject(Shape.Square, transform.position);
    //}

    //protected override void Test3(InputAction.CallbackContext context)
    //{
    //    PoolManager.Inst.GetObject(Shape.Triangle, transform.position);
    //}
}
